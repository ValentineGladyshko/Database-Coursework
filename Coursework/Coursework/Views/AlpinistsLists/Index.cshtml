@model IEnumerable<Coursework.Models.AlpinistsList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AlpinistBases.Country)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Alpinists.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AlpinistBases.Country)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Alpinists.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AlpinistsListID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AlpinistsListID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AlpinistsListID })
        </td>
    </tr>
}

</table>
